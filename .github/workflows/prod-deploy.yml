name: Production - re-deploy

on:
  push:
    branches: [ main ]

jobs:
  run:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: copy repo contents via ssh password
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.PROD_HOSTNAME}}
          key: ${{ secrets.ADMIN_SSH_KEY }}
          source: "*"
          target: "/root/todanni/orchestrate"
          overwrite: true
      -
        name: Run
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.PROD_DO_DROPLET_IP }}
          username: root
          key: ${{ secrets.ADMIN_SSH_KEY }}
          script: |
            docker-compose --file prod-docker-compose.yml down
            docker-compose --file prod-docker-compose.yml up -d
      -
        name: Send discord failure message
        if: ${{ failure() }}
        uses: appleboy/discord-action@master
        with:
          webhook_id: ${{ secrets.DISCORD_WEBHOOK_ID }}
          webhook_token: ${{ secrets.DISCORD_WEBHOOK_TOKEN }}
          color: "#FF0000"
          message: "Failed to run prod images for ${{ github.repository }} \n https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id}}"

      -
        name: Send discord success message
        if: ${{ success() }}
        uses: appleboy/discord-action@master
        with:
          webhook_id: ${{ secrets.DISCORD_WEBHOOK_ID }}
          webhook_token: ${{ secrets.DISCORD_WEBHOOK_TOKEN }}
          color: "#48f442"
          message: Prod is now running latest images for ${{ github.repository }}. \n https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id}}"

